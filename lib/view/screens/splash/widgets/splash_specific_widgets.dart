import 'package:flutter/material.dart';

class SplashSpecificWidget extends StatelessWidget {
  const SplashSpecificWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
